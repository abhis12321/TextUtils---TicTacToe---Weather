import React from 'react'

export default function tApp() {
  return (
    <div>
      {/* <Nav
        title="TextUtils"
        mode={mode}
        toggleMode={toggleMode}
        handle={handleAlert}
        alert={alert}
      ></Nav>

      <Alert alert={alert} />

      <div className="container">
        <TextForm mode={mode} handleAlert={handleAlert} alert={alert} />
        <Acordian mode={mode} />
      </div> */}
      {/* <Routes>
          <Route
            path="/"
            element={
              <Nav
                title="TextUtils"
                mode={mode}
                toggleMode={toggleMode}
                handle={handleAlert}
                alert={alert}
              ></Nav>
            }
          >
            <Route
              App
              element={
                <TextForm mode={mode} handleAlert={handleAlert} alert={alert} />
              }
            />
            <Route path="/blogs" element={<Alert alert={alert} />} />
            <Route path="/contact" element={<Acordian mode={mode} />} />
            <Route path="*" element={<NoPage />} />
          </Route>
        </Routes> */}
    </div>
  )
}
